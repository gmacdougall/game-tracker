<h1>Editing <%= singular_table_name %></h1>

<%%= render 'form' %>

<%%= link_to 'Show', @<%= singular_table_name %>, class: 'btn' if can? :show, @<%= singular_table_name %> %>
<%%= link_to 'Back', <%= index_helper %>_path, class: 'btn' if can? :index, :<%= singular_table_name %> %>
